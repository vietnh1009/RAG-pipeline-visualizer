# ╔══════════════════════════════════════════════════════════════════════╗
# ║  RAG Pipeline Visualizer — requirements.txt                         ║
# ║  Python 3.11+ khuyến nghị                                           ║
# ║                                                                      ║
# ║  Nhóm (A) = bắt buộc để chạy app                                    ║
# ║  Nhóm (B) = cài thêm khi cần tính năng tương ứng                    ║
# ╚══════════════════════════════════════════════════════════════════════╝

# ─────────────────────────────────────────────────────
# (A) BẮT BUỘC — Core app
# ─────────────────────────────────────────────────────
streamlit==1.45.1
python-dotenv==1.0.1

# LangChain
langchain-core==0.3.63
langchain-text-splitters==0.3.8
langchain-community==0.3.24
langchain-experimental==0.3.4

# Data / utilities
numpy==1.26.4
pandas==2.2.3
tabulate==0.9.0

# ─────────────────────────────────────────────────────
# (A) LOADER — tối thiểu pypdf + pymupdf + pdfplumber
# ─────────────────────────────────────────────────────
pypdf==5.1.0
pymupdf==1.24.14
pdfplumber==0.11.4

# ─────────────────────────────────────────────────────
# (A) CHUNKING
# ─────────────────────────────────────────────────────
sentence-transformers==3.3.1
rank-bm25==0.2.2
datasketch==1.6.5
nltk==3.9.1

# ─────────────────────────────────────────────────────
# (A) EMBEDDING — OpenAI (mặc định)
# ─────────────────────────────────────────────────────
openai==1.58.1
langchain-openai==0.3.10
langchain-huggingface==0.1.2
fastembed==0.4.2

# ─────────────────────────────────────────────────────
# (A) VECTOR DB — Chroma + FAISS (mặc định)
# ─────────────────────────────────────────────────────
chromadb==0.6.3
langchain-chroma==0.2.2
faiss-cpu==1.9.0.post1

# ─────────────────────────────────────────────────────
# (B) TÙY CHỌN — Loader nâng cao
# ─────────────────────────────────────────────────────

# Marker PDF — Markdown chất lượng cao, bảng & công thức (strategy: marker)
# marker-pdf==1.6.2

# Docling — IBM parser, Markdown output xuất sắc (strategy: docling)
# docling==2.28.0

# Unstructured — OCR + bảng + hình (strategy: unstructured)
# unstructured[pdf]==0.16.12

# ─────────────────────────────────────────────────────
# (B) TÙY CHỌN — Embedding providers khác
# ─────────────────────────────────────────────────────

# Cohere (embed-multilingual-v3.0)
# cohere==5.13.12
# langchain-cohere==0.4.3

# Google Gemini
# google-generativeai==0.8.3
# langchain-google-genai==2.0.11

# Ollama (local)
# ollama==0.4.7
# langchain-ollama==0.2.3

# ─────────────────────────────────────────────────────
# (B) TÙY CHỌN — Vector DB khác
# ─────────────────────────────────────────────────────

# Qdrant (production, filtering phức tạp)
# qdrant-client==1.13.3
# langchain-qdrant==0.2.0

# LanceDB (embedded, không cần server)
# lancedb==0.17.0

# Weaviate (hybrid out-of-the-box)
# weaviate-client==4.10.4
# langchain-weaviate==0.0.3

# PGVector (PostgreSQL)
# psycopg[binary]==3.2.4
# langchain-postgres==0.0.13

# Pinecone (managed cloud)
# pinecone-client==5.0.1
# langchain-pinecone==0.2.0

# ─────────────────────────────────────────────────────
# (B) TÙY CHỌN — Generation providers khác
# ─────────────────────────────────────────────────────

# Anthropic (Claude)
# anthropic==0.43.0

# Google Gemini — đã có google-generativeai

# Ollama local — đã có ollama

# Cohere — đã có cohere

# ─────────────────────────────────────────────────────
# (B) TÙY CHỌN — Sparse embedding (Hybrid retrieval)
# ─────────────────────────────────────────────────────

# SPLADE sparse neural (tốt hơn BM25, cần ~500MB model)
# transformers==4.47.1
# torch==2.5.1
